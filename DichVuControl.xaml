<UserControl x:Class="DoAnCN_Net.UI.DichVuControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#F4F6FA">

    <UserControl.Resources>
        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background"   Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000" Opacity="0.06" BlurRadius="16" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"      Value="#F8F9FA"/>
            <Setter Property="Foreground"      Value="#555"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Padding"         Value="12,0"/>
            <Setter Property="BorderBrush"     Value="#EEEEEE"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EEF2FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E8EDFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding"           Value="12,0"/>
            <Setter Property="BorderThickness"   Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#333"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="5" Height="28" Background="#e94560"
                        CornerRadius="3" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="Quản lý Dịch vụ"
                               FontSize="22" FontWeight="Bold" Foreground="#1a1a2e"/>
                    <TextBlock Text="Danh sách dịch vụ tổ chức sự kiện"
                               FontSize="12" Foreground="#999" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>

            <Button HorizontalAlignment="Right"
                    Style="{StaticResource Btn}"
                    Background="#e94560" Foreground="White"
                    Padding="16,9" Click="btnAdd_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="＋" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Thêm dịch vụ" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- STATS -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Tổng dịch vụ"   FontSize="12" Foreground="#999"/>
                        <TextBlock x:Name="txtTong"       Text="0"
                                   FontSize="30" FontWeight="Bold" Foreground="#1a1a2e" Margin="0,4,0,0"/>
                        <TextBlock Text="dịch vụ"         FontSize="12" Foreground="#BBB"/>
                    </StackPanel>
                    <Border Width="44" Height="44" CornerRadius="22" Background="#FFF0F3"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="⭐" FontSize="22"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Giá thấp nhất" FontSize="12" Foreground="#999"/>
                        <TextBlock x:Name="txtGiaMin"   Text="—"
                                   FontSize="22" FontWeight="Bold" Foreground="#2ecc71" Margin="0,4,0,0"/>
                        <TextBlock Text="đồng"          FontSize="12" Foreground="#BBB"/>
                    </StackPanel>
                    <Border Width="44" Height="44" CornerRadius="22" Background="#E8F8F0"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="📉" FontSize="22"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource Card}" Padding="20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Giá cao nhất" FontSize="12" Foreground="#999"/>
                        <TextBlock x:Name="txtGiaMax"  Text="—"
                                   FontSize="22" FontWeight="Bold" Foreground="#e94560" Margin="0,4,0,0"/>
                        <TextBlock Text="đồng"         FontSize="12" Foreground="#BBB"/>
                    </StackPanel>
                    <Border Width="44" Height="44" CornerRadius="22" Background="#FFF0F3"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="📈" FontSize="22"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- TABLE CARD -->
        <Border Grid.Row="4" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Border Grid.Row="0" Padding="14,12"
                        BorderBrush="#F0F0F0" BorderThickness="0,0,0,1">
                    <Grid>
                        <Border Background="#F8F9FA" CornerRadius="7"
                                Width="240" Height="36" HorizontalAlignment="Left">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🔍" FontSize="14"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox x:Name="txtSearch" Grid.Column="1"
                                         Background="Transparent" BorderThickness="0"
                                         FontSize="13" Foreground="#555"
                                         VerticalAlignment="Center"
                                         TextChanged="txtSearch_TextChanged"/>
                            </Grid>
                        </Border>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Style="{StaticResource Btn}"
                                    Background="#f39c12" Foreground="White"
                                    Padding="12,8" Margin="0,0,8,0"
                                    Click="btnEdit_Click">
                                <TextBlock Text="✏  Sửa"/>
                            </Button>
                            <Button Style="{StaticResource Btn}"
                                    Background="#e74c3c" Foreground="White"
                                    Padding="12,8" Click="btnDelete_Click">
                                <TextBlock Text="🗑  Xóa"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- DataGrid -->
                <DataGrid x:Name="dgDichVu" Grid.Row="1"
                          Background="White" BorderThickness="0"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F0F0F0"
                          FontSize="13" RowHeight="44" ColumnHeaderHeight="40"
                          SelectionMode="Single" SelectionUnit="FullRow"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#FAFBFC">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã"          Binding="{Binding MaDichVu}"  Width="60"/>
                        <DataGridTextColumn Header="Tên dịch vụ" Binding="{Binding TenDichVu}" Width="*"/>
                        <DataGridTemplateColumn Header="Giá" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock FontWeight="SemiBold" Foreground="#e94560"
                                               VerticalAlignment="Center">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0:N0}đ">
                                                <Binding Path="Gia"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Đã dùng" Binding="{Binding SoSuKien}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight"          Value="SemiBold"/>
                                    <Setter Property="Foreground"          Value="#0f3460"/>
                                    <Setter Property="VerticalAlignment"   Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>