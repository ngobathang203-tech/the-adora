<UserControl x:Class="DoAnCN_Net.UI.MenuAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#F4F6FA">

    <UserControl.Resources>

        <!-- ── Buttons ──────────────────────────────────────────────── -->
        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnRed" BasedOn="{StaticResource Btn}" TargetType="Button">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding"    Value="16,9"/>
        </Style>
        <Style x:Key="BtnOrange" BasedOn="{StaticResource Btn}" TargetType="Button">
            <Setter Property="Background" Value="#f39c12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding"    Value="12,8"/>
        </Style>
        <Style x:Key="BtnDanger" BasedOn="{StaticResource Btn}" TargetType="Button">
            <Setter Property="Background" Value="#e74c3c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding"    Value="12,8"/>
        </Style>

        <!-- ── Card ─────────────────────────────────────────────────── -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background"   Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000" Opacity="0.06" BlurRadius="16" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── DataGrid ──────────────────────────────────────────────── -->
        <Style x:Key="DG" TargetType="DataGrid">
            <Setter Property="Background"               Value="White"/>
            <Setter Property="BorderThickness"          Value="0"/>
            <Setter Property="RowBackground"            Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFBFC"/>
            <Setter Property="GridLinesVisibility"      Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0"/>
            <Setter Property="FontSize"                 Value="13"/>
            <Setter Property="ColumnHeaderHeight"       Value="40"/>
            <Setter Property="RowHeight"                Value="44"/>
            <Setter Property="SelectionMode"            Value="Single"/>
            <Setter Property="AutoGenerateColumns"      Value="False"/>
            <Setter Property="IsReadOnly"               Value="True"/>
            <Setter Property="CanUserAddRows"           Value="False"/>
            <Setter Property="CanUserDeleteRows"        Value="False"/>
            <Setter Property="CanUserResizeRows"        Value="False"/>
            <Setter Property="HeadersVisibility"        Value="Column"/>
            <Setter Property="SelectionUnit"            Value="FullRow"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"      Value="#F8F9FA"/>
            <Setter Property="Foreground"      Value="#555"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Padding"         Value="12,0"/>
            <Setter Property="BorderBrush"     Value="#EEEEEE"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EEF2FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E8EDFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding"           Value="12,0"/>
            <Setter Property="BorderThickness"   Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Xóa hoàn toàn style SearchBox và SearchBoxWithPlaceholder cũ, thay bằng: -->

        <!-- Base style cho TextBox không có placeholder -->
        <Style x:Key="SearchBoxBase" TargetType="TextBox">
            <Setter Property="Background"        Value="Transparent"/>
            <Setter Property="BorderThickness"   Value="0"/>
            <Setter Property="FontSize"          Value="13"/>
            <Setter Property="Foreground"        Value="#555"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Style có placeholder - KHÔNG dùng BasedOn để tránh lỗi -->
        <Style x:Key="SearchBoxWithPlaceholder" TargetType="TextBox">
            <Setter Property="Background"        Value="Transparent"/>
            <Setter Property="BorderThickness"   Value="0"/>
            <Setter Property="FontSize"          Value="13"/>
            <Setter Property="Foreground"        Value="#555"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost" />
                            <TextBlock x:Name="Placeholder"
                               Text="Tìm kiếm menu..."
                               Foreground="#BBBBBB"
                               FontSize="13"
                               VerticalAlignment="Center"
                               Margin="2,0,0,0"
                               IsHitTestVisible="False"
                               Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- ════════════════════════════════════════════════════════════════
         ROOT
    ════════════════════════════════════════════════════════════════ -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── HEADER ──────────────────────────────────────────────── -->
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="5" Height="28" Background="#e94560"
                        CornerRadius="3" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="Quản lý Menu"
                               FontSize="22" FontWeight="Bold" Foreground="#1a1a2e"/>
                    <TextBlock Text="Danh sách thực đơn và chi tiết món ăn"
                               FontSize="12" Foreground="#999" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>

            <Button Style="{StaticResource BtnRed}"
                    HorizontalAlignment="Right"
                    Click="btnAddMenu_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="＋" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Thêm menu mới" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- ── STATS ───────────────────────────────────────────────── -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Tổng -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Tổng số menu"   FontSize="12" Foreground="#999"/>
                        <TextBlock x:Name="txtTotalMenu" Text="0"
                                   FontSize="30" FontWeight="Bold" Foreground="#1a1a2e" Margin="0,4,0,0"/>
                        <TextBlock Text="thực đơn"       FontSize="12" Foreground="#BBB"/>
                    </StackPanel>
                    <Border Width="44" Height="44" CornerRadius="22" Background="#FFF0F3"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="📋" FontSize="22"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Chay -->
            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Menu Chay"     FontSize="12" Foreground="#999"/>
                        <TextBlock x:Name="txtMenuChay" Text="0"
                                   FontSize="30" FontWeight="Bold" Foreground="#2ecc71" Margin="0,4,0,0"/>
                        <TextBlock Text="thực đơn chay" FontSize="12" Foreground="#BBB"/>
                    </StackPanel>
                    <Border Width="44" Height="44" CornerRadius="22" Background="#E8F8F0"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="🥗" FontSize="22"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Mặn -->
            <Border Grid.Column="4" Style="{StaticResource Card}" Padding="20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Menu Mặn"     FontSize="12" Foreground="#999"/>
                        <TextBlock x:Name="txtMenuMan" Text="0"
                                   FontSize="30" FontWeight="Bold" Foreground="#e94560" Margin="0,4,0,0"/>
                        <TextBlock Text="thực đơn mặn" FontSize="12" Foreground="#BBB"/>
                    </StackPanel>
                    <Border Width="44" Height="44" CornerRadius="22" Background="#FFF0F3"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="🍖" FontSize="22"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- ── MAIN CONTENT ────────────────────────────────────────── -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="370"/>
            </Grid.ColumnDefinitions>

            <!-- ══ LEFT: Danh sách menu ══ -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Toolbar -->
                    <Border Grid.Row="0" Padding="14,12"
                            BorderBrush="#F0F0F0" BorderThickness="0,0,0,1">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <!-- Search box: dùng style đã tách riêng, không set Style 2 lần -->
                                <Border Background="#F8F9FA" CornerRadius="7"
                                        Width="210" Height="36">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="36"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="🔍" FontSize="14"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <!-- FIX: Chỉ dùng Style một lần qua key SearchBoxWithPlaceholder -->
                                        <TextBox x:Name="txtSearch" Grid.Column="1"
                                                 Style="{StaticResource SearchBoxWithPlaceholder}"
                                                 TextChanged="txtSearch_TextChanged"/>
                                    </Grid>
                                </Border>

                                <!-- Filter -->
                                <ComboBox x:Name="cboFilter"
                                          Width="120" Height="36" Margin="10,0,0,0"
                                          FontSize="13" Padding="10,6"
                                          SelectionChanged="cboFilter_SelectionChanged">
                                    <ComboBoxItem Content="Tất cả" IsSelected="True"/>
                                    <ComboBoxItem Content="Chay"/>
                                    <ComboBoxItem Content="Mặn"/>
                                </ComboBox>
                            </StackPanel>

                            <!-- Actions -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Style="{StaticResource BtnOrange}"
                                        Click="btnEdit_Click" Margin="0,0,8,0">
                                    <TextBlock Text="✏  Sửa"/>
                                </Button>
                                <Button Style="{StaticResource BtnDanger}"
                                        Click="btnDelete_Click">
                                    <TextBlock Text="🗑  Xóa"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- DataGrid menu -->
                    <DataGrid x:Name="dgMenu" Grid.Row="1"
                              Style="{StaticResource DG}"
                              SelectionChanged="dgMenu_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã"       Binding="{Binding MaMenu}"  Width="55"/>
                            <DataGridTextColumn Header="Tên menu" Binding="{Binding TenMenu}" Width="*"/>

                            <!-- Badge Loại -->
                            <DataGridTemplateColumn Header="Loại" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="12" Padding="8,3"
                                                HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#FFF3E0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding LoaiMenu}" Value="Chay">
                                                            <Setter Property="Background" Value="#E8F5E9"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontSize="12" FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#E67E22"/>
                                                        <Setter Property="Text"       Value="{Binding LoaiMenu}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LoaiMenu}" Value="Chay">
                                                                <Setter Property="Foreground" Value="#27AE60"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Số món -->
                            <DataGridTemplateColumn Header="Số món" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock x:Name="txtSoMon" Text="..."
                                                   HorizontalAlignment="Center"
                                                   FontWeight="SemiBold" Foreground="#0f3460"
                                                   VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- ══ RIGHT: Chi tiết menu ══ -->
            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Dark header -->
                    <Border Grid.Row="0" Background="#1a1a2e"
                            CornerRadius="10,10,0,0" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="Chi tiết menu"
                                       FontSize="13" FontWeight="Bold" Foreground="White"/>
                            <TextBlock x:Name="txtDetailName"
                                       Text="Chọn một menu để xem chi tiết"
                                       FontSize="12" Foreground="#888" Margin="0,4,0,0"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <!-- Badges -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="16,12,16,0">
                        <Border x:Name="bdgLoai" CornerRadius="12" Padding="10,4" Background="#F8F9FA">
                            <TextBlock x:Name="txtDetailLoai" Text="—"
                   FontSize="12" FontWeight="SemiBold" Foreground="#999"/>
                        </Border>
                        <Border CornerRadius="12" Padding="10,4"
            Background="#EEF2FF" Margin="8,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="txtDetailSoMon" Text="0"
                       FontSize="12" FontWeight="Bold" Foreground="#0f3460"/>
                                <TextBlock Text=" món ăn" FontSize="12" Foreground="#0f3460"/>
                            </StackPanel>
                        </Border>

                        <!-- NÚT MỚI: Quản lý món -->
                        <Button Style="{StaticResource BtnOrange}"
            Margin="8,0,0,0" Padding="10,4"
            Click="btnChiTiet_Click">
                            <TextBlock Text="✏ Quản lý món" FontSize="12"/>
                        </Button>
                    </StackPanel>

                    <!-- Danh sách món ăn -->
                    <DataGrid x:Name="dgMonAn" Grid.Row="2"
                              Style="{StaticResource DG}" Margin="0,8,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên món" Binding="{Binding TenMon}" Width="*"/>
                            <DataGridTemplateColumn Header="Giá" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock FontWeight="SemiBold" Foreground="#e94560"
                                                   VerticalAlignment="Center">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0:N0}đ">
                                                    <Binding Path="Gia"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Footer tổng giá -->
                    <Border Grid.Row="3"
                            BorderBrush="#F0F0F0" BorderThickness="0,1,0,0"
                            Padding="16,12">
                        <Grid>
                            <TextBlock Text="Tổng giá trị menu:"
                                       FontSize="13" Foreground="#888" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtTongGia" Text="—"
                                       FontSize="16" FontWeight="Bold" Foreground="#e94560"
                                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
